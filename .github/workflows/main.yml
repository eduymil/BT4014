name: Run Bandit Simulations and Save Output

on:
  workflow_dispatch:
  push:
    paths:
      - "run_simulation.py"
      - "BT4014_Plotting.ipynb"
      - "requirements.txt"

permissions:
  contents: write  # Crucial for the bot to push changes

jobs:
  run-simulations:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        algorithm: [eps, linucb, ts]
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
      - name: Install dependencies
        run: |
          pip install -r requirements.txt
          pip install scikit-learn pandas numpy
      - name: Run Simulation
        run: python run_simulation.py ${{ matrix.algorithm }}
      - uses: actions/upload-artifact@v4
        with:
          name: results-${{ matrix.algorithm }}
          path: results_${{ matrix.algorithm }}.csv

  plot-and-commit:
    needs: run-simulations
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
      - name: Install dependencies
        run: |
          pip install -r requirements.txt
          pip install jupyter nbconvert matplotlib pandas numpy
      - uses: actions/download-artifact@v4
        with:
          pattern: results-*
          merge-multiple: true
      - name: Execute Plotting Notebook
        run: |
          jupyter nbconvert \
            --to notebook \
            --execute BT4014_Plotting.ipynb \
            --inplace \
            --ExecutePreprocessor.kernel_name=python3
      - name: Commit and Push Results
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          # -f forces the add even if results_*.csv is in .gitignore
          git add -f results_*.csv
          git add BT4014_Plotting.ipynb
          if git diff --cached --quiet; then
            echo "No changes to commit"
          else
            git commit -m "Auto-update simulation results [skip ci]"
            git push origin main
          fi